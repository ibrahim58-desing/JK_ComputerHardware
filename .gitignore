# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
.env*.local

# Common ignores
node_modules
.next/
.DS_Store

# Environment
.env
.env.local
.env.production

# Database
prisma/*.db
*.sql

# Uploads — each app (admin-interface, backend-api, user-interface) has its
# own public/uploads/, not just one at the repo root; the leading-slash form
# only matched the latter, so uploaded product photos were being committed.
**/public/uploads/